<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=name %></title>
  </head>
  <body>
    <%- include('./partials/nav') %>
    <h1>Home Page</h1>
    <p><%=name%></p>
    <p><%=address%></p>
    <p><%=phone %></p>
    <p><%=isOpen === true ? "Yes, we're open" : "Sorry, we're closed" %></p>
  </body>
</html>
