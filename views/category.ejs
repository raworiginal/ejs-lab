<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=name%></title>
  </head>
  <body>
    <%- include('./partials/nav') %>
    <h1><%=name%></h1>
    <ul>
      <% items.forEach((dish)=>{ %>
      <li>
        <h3><%= dish.name %></h3>
        <h4>
          $<%= dish.price %> | Rating: <%= dish.rating %><%=dish.rating===5 ?
          "⭐":'' %>
        </h4>
        <p><%=dish.details %></p>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
